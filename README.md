# Лабораторная работа hw1 — Алгоритмы на JS + Three.js

> **Тема:** «Задачи на циклы, массивы, коллекции, объекты на JS» + интеграция функций
> в реальное веб-приложение «Sber Cashback».
> **Курс:** «Программирование сетевых приложений» (БМСТУ).

---

## 📌 Что было сделано

В рамках работы реализованы алгоритмические задачи из методички (`hw.md`)
и встроены в готовый интерфейс кешбэк-сервиса. Каждая функция имеет
прикладное применение в UI: подсчёт суммы транзакций, поиск максимальной
полосы кешбэк-дней, слияние конфигураций аккаунта и т.д.

Дополнительно:

- 🎨 Многостраничное SPA на чистом JS (без фреймворков) с роутингом через классы-страницы.
- 🧩 Компонентная архитектура (`components/`).
- 🪙 3D-сцена с монетой (Three.js + GLTFLoader + OrbitControls).
- 💾 Сохранение активированного процента в `localStorage`.
- 🔔 Toast-уведомления о статусе операции.
- 🖥️ **Алгоритмические задачи запускаются как Node.js CLI (`npm run cli`)
  с вводом из терминала** через `readline`.

---

## 🧮 Реализованные задачи

| № задачи | Уровень | Функция в `cashbackUtils.js`                    | Где применена в UI                              |
| -------- | ------- | ----------------------------------------------- | ----------------------------------------------- |
| 1.4      | 1       | `calculateTransactionStats(amounts)`            | Блок «Обороты за неделю / Индекс лояльности»    |
| 1.9      | 1       | `initCashbackHistory(count, data)`              | Инициализация истории начислений (`console.log`)|
| 2.3      | 2       | `getMaxCashbackStreak(streakStr)`               | «Максимальная полоса активных дней»             |
| 3.1      | 3       | `mergeAccountConfigs(...configs)`               | Объединение настроек аккаунта                   |
| —        | —       | `simulateProcessing(limit)` *(do…while)*        | Имитация обработки транзакций                   |

### Краткая теория

#### Задача 1.4 — сумма и произведение
Линейный обход массива за **O(n)** времени и **O(1)** дополнительной памяти.
Произведение инициализируется единицей (нейтральный элемент умножения),
сумма — нулём.

```js
let sum = 0, mult = 1;
for (const v of arr) { sum += v; mult *= v; }
```

#### Задача 1.9 — заполнение массива
Создание массива фиксированной длины и заполнение его одним значением.
Эквивалент `Array(count).fill(data)`, но реализован вручную для отработки циклов.

#### Задача 2.3 — максимальная подпоследовательность единиц
Классическая задача со «скользящим окном». Поддерживаем два счётчика:
текущая длина серии и максимальная встретившаяся длина.

| Тип цикла  | Сложность | Доп. память |
| ---------- | --------- | ----------- |
| `for…of`   | O(n)      | O(1)        |

#### Задача 3.1 — слияние объектов
Перебор полей всех объектов слева направо. Поле фиксируется только при
**первом** его появлении — последующие игнорируются. Это делает функцию
идеальной для системы «default → user → override» наоборот: первый
переданный объект имеет приоритет.

```js
mergeAccountConfigs(userConfig, defaults) // user wins
```

#### Бонус — `do…while`
Цикл с пост-условием: тело гарантированно выполнится хотя бы один раз.
Используется для имитации синхронной обработки транзакций.

---

## 🗂 Структура проекта

```
.
├── cli.js                      # ★ Node.js CLI: ввод задач из терминала
├── index.html                  # Точка входа SPA
├── main.js                     # Инициализация SPA-роутера
├── style.css                   # Кастомные стили поверх Bootstrap
│
├── pages/
│   ├── main/index.js           # Главная: сетка 4 категорий кешбэка
│   └── product/index.js        # Детальная: 3D-сцена + статистика
│
├── components/
│   ├── product-card/           # Карточка категории
│   ├── product/                # Детальная панель товара
│   ├── back-button/            # Кнопка «Назад»
│   └── toast/                  # Toast-уведомление
│
├── utils/
│   └── cashbackUtils.js        # ★ Чистые функции задач (импортятся в Node и в браузер)
│
├── assets/                     # PNG-иконки категорий
└── docs/                       # Спецификация системы
```

---

## 🖥️ Node.js CLI

Все задачи можно прогнать из терминала, не открывая браузер:

```bash
npm run cli
# или
node cli.js
```

Скрипт последовательно задаёт 7 вопросов через `readline/promises`.
Пустая строка = значение по умолчанию.

### Пример сессии

```
$ npm run cli
=== Task 2.3 — максимальная серия активных дней ===
Строка из 0 и 1 [1110110111101]: 11011110111
  → max streak = 4

=== Task 1.4 — сумма и произведение транзакций ===
Суммы через запятую [100, 200, 150, 300]: 50, 75, 120
  → sum  = 245
  → mult = 450000

=== Task 3.1 — слияние конфигов ===
Первый конфиг JSON [...]: {"theme":"dark"}
Второй конфиг JSON [...]: {"theme":"light","region":"RU"}
  → merged = { theme: 'dark', region: 'RU' }

=== Task 1.9 — заполнение истории ===
Длина истории [5]: 3
Значение для заполнения [Pending]: Done
  → history = [ 'Done', 'Done', 'Done' ]

=== Бонус — do…while: имитация обработки ===
Сколько транзакций обработать [3]: 3
Обработка транзакции 1...
Обработка транзакции 2...
Обработка транзакции 3...
```

### Работа в pipe-режиме

Скрипт детектит TTY и при подаче stdin через pipe буферизует
весь ввод сразу, а потом раздаёт строки по запросам. Это позволяет
запускать его в скриптах:

```bash
printf '11011110111\n50, 75, 120\n\n\n3\nDone\n3\n' | node cli.js
```

---

## ▶️ Запуск

### Веб-приложение

```bash
npm install
npx serve .                 # http://localhost:3000
# или
python3 -m http.server 5500 # http://localhost:5500
```

UI работает с демонстрационными данными — карточки кешбэка, 3D-монета,
блок статистики (с захардкоженной демо-серией).

### Алгоритмические задачи

```bash
npm run cli
```

---

## 🧪 Сценарий проверки

### CLI

| Шаг | Ввод                          | Ожидание                              |
| --- | ----------------------------- | ------------------------------------- |
| 1   | `11011110111`                 | `max streak = 4`                      |
| 2   | `50, 75, 120`                 | `sum = 245`, `mult = 450000`          |
| 3   | `{"theme":"dark"}` + дефолт   | `merged.theme === 'dark'`             |
| 4   | `3` + `Done`                  | `['Done', 'Done', 'Done']`            |
| 5   | `3`                           | 3 строки «Обработка транзакции…»      |

### UI

| Шаг | Действие                                   | Ожидание                          |
| --- | ------------------------------------------ | --------------------------------- |
| 1   | Открыть `/`                                | 4 карточки кешбэка                |
| 2   | Кликнуть «Рестораны и кафе»                | Детальная страница + 3D-монета    |
| 3   | Ввести `25`, нажать «Активировать»         | Toast «Категория активирована»    |
| 4   | Перезагрузить страницу                     | Процент `25` сохранён в badge     |

---

## 🛠 Технологии

| Категория       | Стек                                |
| --------------- | ----------------------------------- |
| UI              | HTML5, Bootstrap 5, кастомный CSS   |
| Логика          | Vanilla JS (ES Modules)             |
| 3D              | Three.js 0.160 + GLTFLoader         |
| Управление 3D   | OrbitControls                       |
| Хранение        | `localStorage`                      |
| Ввод данных     | Node.js `readline/promises` *(stdin терминала)* |

---

## 📚 Связанные ветки

| Ветка               | Содержание                                 |
| ------------------- | ------------------------------------------ |
| `main`              | Стартовая ветка с обзором всех работ       |
| `calculator-layout` | Лаб. 1: вёрстка калькулятора               |
| `calc-implementation` | Лаб. 1: реализация логики калькулятора   |
| `cards-layout`      | Лаб. 2: вёрстка cashback-карточек          |
| `three-js-gallery`  | Лаб. 3: 3D-галерея                         |
| **`hw1`** *(эта)*   | ДЗ: алгоритмы JS + интеграция в UI         |
| `fetch-lab`         | Лаб. 4: AJAX/Fetch                         |
| `cors-lab`          | Лаб. 5: CORS                               |
| `express-js-cards`  | Лаб. 6: Express.js backend                 |
